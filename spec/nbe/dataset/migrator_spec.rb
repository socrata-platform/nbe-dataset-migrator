require 'spec_helper'

describe Nbe::Dataset::Migrator do
  it 'has a version number' do
    expect(Nbe::Dataset::Migrator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
